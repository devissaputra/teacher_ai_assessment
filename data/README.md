# Data policy

This repository ships without real learner or teacher data. Run `python examples/demo.py` to generate synthetic demonstration data in `results/`.

For a real study, define consent, retention, access control, de-identification, and deletion procedures before collection. The variables described in `docs/data_dictionary.md` are a proposed schema, not permission to collect them.
